apiVersion: v1
kind: Service
metadata:
  name: {{ include "nitro.fullname" . }}
  labels:
    {{- include "nitro.labels" . | nindent 4 }}
    function: nitro-svc
spec:
  type: {{ .Values.bidValidator.service.type }}
  ports:
    {{- if and .Values.bidValidator.configmap.data.http .Values.bidValidator.configmap.data.http.port }}
    - port: {{ index .Values.bidValidator.configmap.data.http.port }}
      targetPort: http-rpc
      protocol: TCP
      name: http-rpc
    {{- end }}
    {{- if and .Values.bidValidator.configmap.data.ws .Values.bidValidator.configmap.data.ws.port }}
    - port: {{ index .Values.bidValidator.configmap.data.ws.port }}
      targetPort: ws
      protocol: TCP
      name: ws
    {{- end }}
      {{- if .Values.bidValidator.auth.enabled }}
    - port: {{ .Values.bidValidator.auth.port }}
      targetPort: {{ .Values.bidValidator.auth.port }}
      protocol: TCP
      name: auth
  {{- end }}
    {{- if .Values.bidValidator.configmap.data.metrics }}
    - port: {{ index .Values "configmap" "data" "metrics-server" "port" }}
      targetPort: metrics
      protocol: TCP
      name: metrics
    {{- end }}
    {{- range .Values.bidValidator.extraPorts }}
    {{- if .serviceEnabled | default true }}
    - name: {{ .name }}
      port: {{ .servicePort | default .port }}
      targetPort: {{ .port }}
      protocol: {{ .protocol | default "TCP" }}
    {{- end }}
    {{- end }}
  selector:
    {{- include "nitro.selectorLabels" . | nindent 4 }}
    function: nitro
  publishNotReadyAddresses: {{ .Values.bidValidator.service.publishNotReadyAddresses }}
